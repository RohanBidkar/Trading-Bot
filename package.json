{
  "name": "alert-bot",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker that lets Telegram trigger the oversold scanner workflow",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secrets": "wrangler secret list"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
